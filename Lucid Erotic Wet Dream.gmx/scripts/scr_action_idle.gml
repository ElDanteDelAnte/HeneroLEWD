///Idle Animation
image_speed = imgSpeed_idle;
curvel = 0;

//if in grass, sprIdleGrass[ori]
//else,
    //only sets sprite index once
    if (sprite_index != sprIdle[ori])
        sprite_index = sprIdle[ori];


